{
  "name": "@rafiki270/teams",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "npm run build",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "geoip-lite": "^1.4.10"
  },
  "peerDependencies": {
    "fastify": "^4.0.0",
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "@types/geoip-lite": "^1.4.4",
    "@types/node": "^22.10.1",
    "fastify": "^4.29.0",
    "typescript": "^5.7.2",
    "vitest": "^1.6.0"
  }
}
